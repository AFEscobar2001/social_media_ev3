# Dependencias de la API REST de sentimiento (EV3 SCY1101)
# Núcleo (ligero): la API arranca y /predict funciona solo con esto.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
scikit-learn==1.5.1
pandas==2.2.2
joblib==1.4.2

# --- OPCIONAL: modelo de referencia real (endpoints /compare y /benchmark) ---
# tweetnlp arrastra torch + transformers (~varios cientos de MB) y descarga el
# modelo desde huggingface.co la primera vez. Descomentar solo si vas a usar la
# comparación contra el modelo real de Cardiff NLP.
# tweetnlp==0.4.4

# --- Solo para tests ---------------------------------------------------------
# pytest==8.2.2
# httpx==0.27.0